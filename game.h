#include "classes.h"

#ifndef __game_h__
#define __game_h__

node* game(node*head, char * aux_nome);
void joker(node*head, int*);
unsigned int level_check (node* aux,int level);
void printScore(char * aux_nome);
#endif

